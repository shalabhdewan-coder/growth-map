# Sarah — Maths — Rung 11 "Competition heuristics"

**Blended grade-equivalent target:** Math Kangaroo Benjamin (upper tier) / SASMO upper tiers — per the plan's rung-map table, "the step just below rung 12's already-built AMC 8-entry ceiling." This rung's entire job is to be a real, measurable step up from rung 10 and a real, measurable step down from rung 12 — the audit below is written with that ordering constraint front and center.

## Source check — what was actually read

**`sources/maths/nrich/CreatingExpressionsStage3Star1Sheet1.pdf`** (already read in full for rung 9) — the harder item in this sheet, **"Adding and Multiplying"** (*"Amy was asked to multiply a number by 8 and then add 5. Instead, she added 8 to the number and then multiplied the result by 5. This gave her 2015. What would the answer have been if Amy had followed the instructions correctly?"*), is the direct model for this rung's new `CHAL.comp` working-backwards item — rung 9 got the simpler single-path version of this skill; rung 11 gets the full two-path reverse-engineering version, which is a genuine, sourced escalation between the two rungs.

**`sources/maths/nrich/EquationsStage3Star1Sheet1.pdf`** — **"Partial Magic"** (magic square, row/column/diagonal totals) is the direct model for this rung's magic-square generator (built as an `algebra>=3` tier so it also automatically appears at rung 10's neighbour rung 12, see below) — already fully cited in rung 10's note; not re-quoted here beyond the citation.

**`sources/maths/mathcounts/sample-school-sprint.pdf`** (official MATHCOUNTS Sprint Round sample, nominally grade 6-8 but explicitly authorized for this stretch range per the task) — read Problems 1-8. Confirmed the same calibration pattern already found in the AMC 8 compendium for rung 12: **the first several Sprint-round problems are genuinely easy** ("How many centimeters long is the washi tape shown on a ruler," "tens digit of 12,345+56,789," "how many more ounces does Dora drink than Erika") — pure arithmetic, no algebra notation. This corroborates the rung-map's framing that MATHCOUNTS is "usable for this stretch range" specifically at its easy end, and confirms this rung should stay anchored to reasoning/heuristic problems (working-backwards, consecutive-integer sums, CRT-style remainders) rather than reaching for MATHCOUNTS' harder algebraic-manipulation problems, which belong above rung 12, not at rung 11.

## Sarah's own school evidence (from `content/sarah-placement-assessment.md`) — the single most load-bearing source for this rung

The "2nd March" critical-thinking sheet (routine Thursday-Prep track, not a one-off) includes, verbatim from the evidence table: *"Chickens/sheep legs system (Diophantine), seating-arrangement logic puzzle, CRT-style number theory ('divisible by 3&4, div by 5 not 10, remainder 1 mod 4'), inequality counting ('×7 between 200-250, how many answers')"* — rated **"Rung 11-12 equivalent — same difficulty family as AMC8-entry."**

The Jan 17 "Math Asynchronous Task" (also routine, not enrichment) independently confirms the same tier: *"Grid-logic deduction from spatial+colour clues, combinatorics ('4 cards 1-8 summing to 20, find all 7 possibilities'), 3-variable simultaneous-equations word problem (bags of flour), CRT-style remainder puzzle ('between 30-50, mod 4 = 2, mod 5 = 1')"* — again rated **"Rung 10-12 equivalent,"** and the placement assessment explicitly calls the CRT puzzle "functionally identical to rung 12's logic/number-theory additions."

**This is the single clearest piece of evidence in the whole placement file that a CRT-style remainder puzzle belongs specifically at this tier of the ladder** (rung 11, immediately below rung 12's already-built CRT item in `CHAL[6]`) — not invented from general competition-math knowledge, but reproducing the exact puzzle *type* Sarah's own school gives her routinely (with different numbers, since the source is a private homework PDF, not a publishable text — the puzzle *shape* is public-domain modular-arithmetic reasoning, not copyrighted content).

## Verdict on current knobs/generators — dead-flag / narrow-reach audit

`LEVELS.sarah.maths[10]` (before this task) `= {n:'Competition heuristics', algebra:2, geom:1, comp:1, s:3, bar:'ratio', patt:'quad', chal:5}`.

**The core finding for this rung: `L.comp` was a fully dead flag.** Traced every reference to `L.comp` in the codebase before this task — there was exactly **one**, in `ws_maths_sarah`: `'★ Challenge — think hard, take your time'+(L.comp?' (competition-style)':'')`. That is a **label string only** — `L.comp` never once influenced which content was actually generated, anywhere. Rung 11 (`comp:1`, `chal:5`) and rung 10 (no `comp`, also `chal:5`) drew from the **exact same `CHAL[5]` pool** — meaning, before this fix, the only functional difference between rung 10 and rung 11's entire worksheet was `algebra:2` vs `algebra:2` (identical — also a dead-end, see rung 10's note on `m_algebra`'s flat 10-11-12 tier) and `bar:'pct'` vs `bar:'ratio'` (a lateral swap, not an escalation — rung 11's ratio bar was byte-identical in difficulty to rung 7's). **In practice, before this task, rung 11 was barely distinguishable from rung 10 at all**, which is precisely the failure mode the closing-sweep instructions warned about ("no reason rung 11 should... barely differ from rung 8" — this audit found it was closer to true of rung 10).

Also inherited the same **severity-1 `longmul` dead-flag** documented in rung 10's note (rung 11's pre-fix knob object also had no `longmul`, so its warm-up block was equally stuck at rung-1-equivalent difficulty) — fixed identically (see below), not re-derived here to avoid duplicating rung 10's full analysis.

Also inherited the same **non-randomized `patt:'quad'`** bug documented in rung 10's note (fixed once, in the shared `m_pattern` function, benefiting all three rungs 10-12 simultaneously — not re-implemented per-rung).

## Changes made in `growth-map.html`

1. **`LEVELS.sarah.maths[10]`**: 
   - Added `longmul:1` (fixes the warm-up regression, same fix as rung 10).
   - Bumped `algebra:2 → algebra:3` (unlocks the magic-square item in `m_algebra`, see rung 10's note for the generator — this now creates a real escalation: rung 10 = 4 algebra items, rung 11 = 5, matching the rung-map's "just below rung 12" framing since rung 12 was also bumped to `algebra:3`, keeping it a strict superset rather than falling behind).
   - Changed `bar:'ratio' → bar:'ratio3'` — a new three-part ratio-sharing bar model (`m_bar`'s new `ratio3` type: three friends split a total in a three-term ratio, e.g. `1:2:4`), a genuine step up from the existing two-part `ratio` type (used at rung 7 and, before this fix, redundantly at rung 11 too) — three unknowns from one total is a real reasoning increment, not a relabelling.
2. **`m_bar`**: added the `ratio3` type described above. Purely additive — no other rung sets `bar:'ratio3'`.
3. **`CHAL`**: added a new `comp` bank (keyed by the string `'comp'`, sitting alongside the existing numeric-tier keys 1-6 on the same object — valid JS, doesn't collide with any `L.chal` numeric lookup) with three items, each sourced as described above:
   - a CRT-style remainder puzzle (*"A number between 30 and 50 leaves remainder 2 when divided by 5, and remainder 1 when divided by 4. What is the number?"*, answer 37) — direct analogue of Sarah's own routine coursework evidence, deliberately using different numbers from both her homework and from rung 12's existing `CHAL[6]` remainder item (which uses mod 2-6, not mod 4/5) so the two don't read as duplicates.
   - a consecutive-integers-sum puzzle (*"The sum of 9 consecutive whole numbers is 108. What is the largest?"*, answer 16) — modeled on NRICH's "Nine in a Line"/"Seven Dwarfs" shape, deliberately using **9** consecutive numbers and asking for the **largest** (not the middle), to be a genuine step up from `CHAL[5]`'s existing "5 consecutive numbers, find the middle" item rather than a near-duplicate of it.
   - a two-path working-backwards puzzle (*"A student was asked to add 7 to a number, then multiply by 3. Instead they multiplied by 3 first, then added 7, and got 46. What should the correct answer have been?"*, answer 60) — direct model of NRICH's "Adding and Multiplying," described in the source section above.
4. **`ws_maths_sarah`**: the `★ Challenge` block now merges `CHAL.comp` into the drawn pool whenever `L.comp` is set (`let chalPool=CHAL[L.chal]||CHAL[3];if(L.comp&&CHAL.comp)chalPool=chalPool.concat(CHAL.comp);`), making the flag functionally live for the first time. This also ripples into rung 12 (which also sets `comp:1`) — a deliberate, documented, purely-additive improvement (rung 12 gains 3 more possible challenge items on top of its existing `CHAL[6]` pool; nothing removed).
5. **`m_geometry`** and **`m_algebra`**'s `algebra>=3` magic-square tier were built as shared, rung-agnostic generator code in the rung-10 task; this rung's knob changes (`comp:1` already set, `algebra:3` newly set) are what makes rung 11 draw on the shaded-region geometry option and the magic-square algebra item — no additional generator code needed here beyond what rung 10's task already built.

No existing function signatures or return shapes changed beyond what rung 10's note already documents; `ws_maths_sarah`'s call signature is untouched.

## Verification

- `node --check` on the extracted `<script>` block: pass, no output.
- Generated `ws_maths_sarah(10)` (rung 11, 0-indexed) 200 times directly in Node: zero occurrences of `undefined`, `NaN`, or `[object Object]`; block/answer count always matched.
- Confirmed `CHAL.comp` items now appear at roughly the expected rate (111/200 ≈ 55%, consistent with a 3-item addition to rung 11's 3-item base `CHAL[5]` pool, i.e. ~50% expected).
- Confirmed the magic-square item appears in block F on every single trial (200/200 — correct, since it's an unconditional inclusion once `algebra>=3`, matching the existing unconditional-inclusion pattern already used for the `algebra>=1`/`>=2` items).
- Confirmed the geometry block now draws from 5 distinct headers across 200 trials (rectangle / triangle-angle / triangle-area / composite-shape / shaded-region — the last one only reachable because `comp:1` is set).
- Manually verified one generated magic square end-to-end by hand: displayed grid `5 15 13 / ? 11 3 / 9 ? 17`; row 1 sum with a filled first cell of 19 gives 19+11+3=33, matching every other row/column/diagonal total of 33; column 1 gives 5+19+9=33 ✓; the second blank (row 3, col 2) solves to 7 via 9+7+17=33, also matching column 2's 15+11+7=33 ✓ and the anti-diagonal 13+11+9=33 ✓. Confirms the Lo Shu-affine-scaling approach (`grid[i] = base[i]·k + off`, magic constant `= 15k+3·off`) produces a genuinely valid, solvable magic square every time, not just a plausible-looking one.
- Re-ran rungs 1-9, 12 (sharing generators with rung 11) 20 times each: zero `undefined`/`NaN`/`[object Object]`; confirmed rungs 1-9 never draw `CHAL.comp` items or the `ratio3`/shaded-region/magic-square content (none of them set `comp`/`bar:'ratio3'`/`algebra>=3`), and rung 12 correctly gains the `CHAL.comp` items and magic-square item as an intentional, documented ripple (not a regression — rung 12 still has its own separate logic + proof blocks that rung 11 lacks, preserving rung 12 as the overall harder rung despite this shared-content overlap).
- Full 12-rung Sarah-maths sweep (10 samples each, 120 total) after all three rungs' (9, 10, 11) edits were complete: all 12 rungs clean, plus a 10-rung Mirayah-maths sweep (fully separate generators, unaffected): all 10 rungs clean. See the plan's Phase 6 closing-sweep notes for the full end-to-end escalation read across all 12 rungs.
